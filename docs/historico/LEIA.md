# Documentos anteriores a separacao

`README-monorepo.md` e o README do monorepo unico, preservado inteiro porque
descreve decisoes que continuam valendo — papeis de banco, RN11/RN12, trilha de
auditoria, por que o login nao revela se o e-mail existe.

Ele tambem descreve os tres frontends, que nao moram mais aqui. Onde os dois
textos divergirem sobre ESTRUTURA, vale o README da raiz; onde divergirem sobre
COMPORTAMENTO, vale o codigo.

Nenhum comportamento foi alterado na separacao: nem autenticacao, nem reserva,
nem RLS, nem os enums.
